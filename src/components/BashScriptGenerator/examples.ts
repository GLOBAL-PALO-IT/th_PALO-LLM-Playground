export const examplesInstructionForShellAgent = [
  'I am working with kubernetes, Create 3 pods with names nginx1,nginx2,nginx3. All of them should have the label app=v1 and check if they are running through web browser',
  'I am working with kubernetes, delete all the pods and its services',
  'I am working with kubernetes, delete all the running pods',
  'I am working with kubernetes, list all the pods available on my local',
  'Set up a local backend REST API for /products and return a list of 5 products.',
  'Create json-server that has mock claims api and show me the openapi spec',
  'Extract content from a webpage and filter out all the HTML, make it into nice looking markdown file',
]
